export enum DatabaseRk {
	PRODUCT_CREATE = "database.product.create",
	AUCTION_CREATE = "database.auction.create",
	AUCTION_START = "database.auction.start",
	AUCTION_TERMINATE = "database.auction.terminate",
}
